import React from 'react';

const Doudous = () => {
    return (
        <div className="doudous">
            {/* TODO return <DoudouPresentation> components 
                map them with key : _id
                Return all of the doudous in the database with infinite scrolling
                Or a selection given the browseFilter choice
            */}
        </div>
    );
};

export default Doudous;