import React from 'react';
import Banner from '../components/Banner';

const Give = () => {
    return (
        <div className="give">
            <Banner />
        </div>
    );
};

export default Give;